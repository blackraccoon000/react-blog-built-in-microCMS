import React from 'react';
import HeaderStart from '../../components/L2_Molecules/HeaderStart';

export default {
  title: 'L2_Molecules/HeaderStart',
  component: HeaderStart,
};

const Template = (args) => <HeaderStart {...args} />;

export const Default = Template.bind({});
Default.args = {};
