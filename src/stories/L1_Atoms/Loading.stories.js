import React from 'react';
import Loading from '../../components/L1_Atoms/Loading';

export default {
  title: 'L1_Atoms/Loading',
  component: Loading,
};

const Template = (args) => <Loading {...args} />;

export const Default = Template.bind({});
Default.args = {};
